services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      CORS_ORIGIN: "*"
      TURN_TIMER: 60
      SEED_MODE: "off"
      STATIC_DIR: /app/apps/web/dist
    restart: unless-stopped

{
  "name": "property-tycoon",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @pty/engine build && concurrently -n server,web -c green,blue \"pnpm --filter @pty/server dev\" \"pnpm --filter @pty/web dev\"",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
